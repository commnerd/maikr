apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "maikr.fullname" . }}-test-connection"
  labels:
    {{- include "maikr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "maikr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
