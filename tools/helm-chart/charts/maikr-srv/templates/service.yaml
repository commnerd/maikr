apiVersion: v1
kind: Service
metadata:
  name: {{ include "maikr-srv.fullname" . }}
  labels:
    {{- include "maikr-srv.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "maikr-srv.selectorLabels" . | nindent 4 }}
