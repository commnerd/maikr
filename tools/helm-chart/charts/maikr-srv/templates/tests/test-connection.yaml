apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "maikr-srv.fullname" . }}-test-connection"
  labels:
    {{- include "maikr-srv.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "maikr-srv.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
