apiVersion: v1
kind: Service
metadata:
  name: {{ include "maikr.fullname" . }}-ui
  labels:
    {{- include "maikr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.ui.type }}
  ports:
    - port: {{ .Values.service.ui.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "maikr.selectorLabels" . | nindent 4 }}
