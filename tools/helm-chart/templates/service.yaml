apiVersion: v1
kind: Service
metadata:
  name: {{ include "maikr.fullname" . }}
  labels:
    {{- include "maikr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "maikr.selectorLabels" . | nindent 4 }}
